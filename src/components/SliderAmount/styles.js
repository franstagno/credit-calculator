import styled from "styled-components";

const Wrapper = styled.div`
	width: 100%;
`;

const TotalMount = styled.div`
	width: 100%;
	display: flex;
	justify-content: space-between;
`;

const Input = styled.input`
	width: 130px;
	text-align: center;
	padding: 0;
	font-size: 25px;
	font-weight: 700;
`;

const Text = styled.p``;

export { Wrapper, TotalMount, Input, Text };
